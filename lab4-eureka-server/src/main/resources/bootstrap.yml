spring:
  application:
    name: lab-4-eureka-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:4044/
# prevent to acting as a client
    register-with-eureka: false
#Does not register itself in the service registry.
    fetch-registry: false